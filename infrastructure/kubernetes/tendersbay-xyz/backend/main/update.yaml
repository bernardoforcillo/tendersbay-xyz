# 1. ImageRepository
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: tendersbay-backend-image-repository
  namespace: tendersbay-xyz
spec:
  image: bernardoforcillo/tendersbay-backend
  interval: 1m

---
# 2. ImagePolicy (stable)
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: tendersbay-backend-image-policy
  namespace: tendersbay-xyz
spec:
  imageRepositoryRef:
    name: tendersbay-backend-image-repository
  filterTags:
    # Stable tags published from main are <YYYYMMDDHHMMSS>-<full-sha>.
    pattern: "^(?P<timestamp>[0-9]{14})-[0-9a-fA-F]{40}$"
    extract: "$timestamp"
  policy:
    numerical:
      order: asc
  digestReflectionPolicy: Always
  interval: 1m

---
# 3. ImageUpdateAutomation (backend-scoped; commits to main)
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: image-automation-tendersbay-backend
  namespace: tendersbay-xyz
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: tendersbay-xyz-repository
    namespace: flux-system
  git:
    commit:
      author:
        name: Bernardo Forcillo
        email: bernardoforcillo@gmail.com
      messageTemplate: "chore: update image backend"
    push:
      branch: main
  update:
    # Scan the whole backend app folder so both channels get their markers rewritten.
    path: "./infrastructure/kubernetes/tendersbay-xyz/backend"
    strategy: Setters
