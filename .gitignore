# Dependencies
node_modules/
.gocache/

# Build outputs
dist/
build/
out/
.next/
bin/
tmp/
# Turborepo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.*
!.env.example

# Caches
coverage/
*.tsbuildinfo
.gocache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code (keep shared config, ignore personal overrides)
.claude/settings.local.json

# Design specs & implementation plans (docs/superpowers/) and SDD scratch
# (.superpowers/) are local-only working docs — kept on disk, NEVER committed.
docs/superpowers/
.superpowers/

# Browser-automation scratch (screenshots, snapshots) from MCP/Playwright
.playwright-mcp/

# Kubernetes secrets applied out-of-band (real values, NEVER committed).
# The committed *secret.yaml under infrastructure/ are empty templates only.
local.secret.yaml
*.local.secret.yaml
docs/postmortems/*
