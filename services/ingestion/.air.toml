root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/ingestion.exe ."
bin = "./tmp/ingestion.exe"
include_ext = ["go"]
exclude_dir = ["tmp", "bin", "node_modules"]
delay = 200

[log]
time = true
