# Copy to .env (gitignored) and fill in real values for local development.
# scripts/run-development.sh already exports DATABASE_URL, JWT_SECRET, etc. —
# this file only needs to cover secrets that script doesn't set.

FIREWORKS_API_KEY=
