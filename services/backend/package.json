{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o ./bin/backend .",
    "dev": "air",
    "lint": "gofmt -l . && go vet ./...",
    "test": "go test ./..."
  }
}
